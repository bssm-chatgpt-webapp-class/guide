import { HambugIcon, PlusIcon } from "../../icons";

const Header = () => {
  return (
    <div>
      <HambugIcon />
      <div>New chat</div>
      <PlusIcon />
    </div>
  );
};

export default Header;
